
  export interface LoginState {
    email: string;
    password: string;
    isLog: boolean,
    error: string,
    data: any,
  }
  
  export interface LoginRequest {
    email: string;
    password: string;
  }

